import { useEffect,useRef } from 'react';
export function AboutDialog({onClose}:{onClose:()=>void}){const close=useRef<HTMLButtonElement>(null);useEffect(()=>{close.current?.focus();const f=(e:KeyboardEvent)=>e.key==='Escape'&&onClose();document.addEventListener('keydown',f);return()=>document.removeEventListener('keydown',f)},[onClose]);return <div className="dialog-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="about-dialog" role="dialog" aria-modal="true" aria-labelledby="about-title"><button ref={close} className="dialog-close" onClick={onClose} aria-label="关闭关于测试">×</button><p className="kicker">关于这面镜</p><h2 id="about-title">它比较的是作品气质，<br/>不是人的定论。</h2><p>“诗心原型”来自诗人的代表作品、常见表达方式、题材重心和文学史中的稳定文化形象。它不是对历史人物真实心理的还原。</p><p>32 道题分别测量八组双极倾向，每组四题。系统计算你本次回答与 16 位诗人原型之间的等权距离；前两位非常接近时，会显示双原型。</p><p>进度和结果只保存在当前浏览器。没有账号，也不会上传你的回答。题库更新后，旧版本答案不会继续参与计算。</p></section></div>}

